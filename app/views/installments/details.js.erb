$('#dialog h3').html("<%= l(params[:p_day].to_datetime, format: '%B %Y') %>");

$('.modal-body').html('<%= j render("details") %>');

$('#dialog').modal("show");

$('#dialog').on('shown.bs.modal', function () {
  $('.first_input').focus()
})
