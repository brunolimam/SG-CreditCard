<%= form_for '',:html => {:target=>'new_section_frame'}, :url => report_purchases_path(format: :pdf), :method => :post do |f| %>
 <%= render 'order' %>
 <br>
  <div class="actions">
    <%= f.submit "Criar", :class=>"btn btn-success" %>
  </div>
<% end %>